---
phase: 01-maintenance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [index.html, js/modules/tracking.js, style.css]
autonomous: true
requirements: [TRACK-01, TRACK-02, TRACK-03]
must_haves:
  truths:
    - "Train progress bar shows current position"
    - "Station markers display on route map"
    - "Progress updates every 60 seconds"
  artifacts:
    - path: "index.html"
      provides: "Tracking view with station list and progress bar"
      min_lines: 30
    - path: "js/modules/tracking.js"
      provides: "Train tracking logic and station data"
      min_lines: 30
  key_links:
    - from: "tracking.js"
      to: "progress"
      via: "setInterval 60000ms"
---

<objective>
Maintain and improve train tracking functionality
Purpose: Ensure progress bar, station markers, and auto-updates work correctly
Output: All tracking requirements verified functional
</objective>

<execution_context>
@C:/Users/A S U S/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/A S U S/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

From codebase structure:
- js/modules/tracking.js: Contains station data and update logic
- style.css: Tracking progress bar styles
- index.html: Tracking view section
</context>

<tasks>

<task type="auto">
  <name>Verify train progress bar shows current position</name>
  <files>index.html, js/modules/tracking.js</files>
  <action>Verify progress bar element exists in tracking view. Confirm current station indicator is visible and updates based on train position. Check progress calculation is accurate.</action>
  <verify>
    <automated>grep -c "progress" index.html && grep -c "tracking" js/modules/tracking.js</automated>
  </verify>
  <done>Progress bar displays current position with visual indicator</done>
</task>

<task type="auto">
  <name>Verify station markers display on route</name>
  <files>index.html, js/modules/tracking.js, style.css</files>
  <action>Confirm Delhi → Mathura → Agra route is displayed. Verify station markers (dots/icons) show all stations on route. Check spacing and visibility.</action>
  <verify>
    <automated>grep -c "station" js/modules/tracking.js</automated>
  </verify>
  <done>All station markers visible on route with proper labeling</done>
</task>

<task type="auto">
  <name>Verify progress updates every 60 seconds</name>
  <files>js/modules/tracking.js</files>
  <action>Confirm setInterval or timer logic runs every 60000ms (60 seconds). Verify position updates automatically without user interaction. Check auto-update doesn't cause jank.</action>
  <verify>
    <automated>grep "setInterval\|60000\|60000ms" js/modules/tracking.js</automated>
  </verify>
  <done>Progress auto-updates every 60 seconds when tracking view is active</done>
</task>

</tasks>

<verification>
1. Open tracking view
2. Note current station position
3. Wait 60 seconds - verify position advances
4. Check station markers match route
</verification>

<success_criteria>
- Progress bar shows current position
- Station markers display correctly for Delhi-Mathura-Agra route
- Auto-update runs every 60 seconds
- All tracking requirements marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-maintenance/01-02-SUMMARY.md`
</output>