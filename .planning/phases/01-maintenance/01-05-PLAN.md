---
phase: 01-maintenance
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [sw.js, index.html, style.css]
autonomous: true
requirements: [PWA-01, PWA-02]
must_haves:
  truths:
    - "App works offline via service worker"
    - "Installable on mobile devices"
  artifacts:
    - path: "sw.js"
      provides: "Service worker for offline caching"
      min_lines: 30
    - path: "index.html"
      provides: "PWA manifest link and meta tags"
      min_lines: 20
  key_links:
    - from: "sw.js"
      to: "cache"
      via: "install and fetch handlers"
---

<objective>
Maintain and improve PWA functionality
Purpose: Ensure service worker caches properly and app is installable
Output: All PWA requirements verified functional
</objective>

<execution_context>
@C:/Users/A S U S/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/A S U S/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

From codebase structure:
- sw.js: Service worker for offline support
- index.html: PWA meta tags and manifest reference
- style.css: App icon styles (if installed)
</context>

<tasks>

<task type="auto">
  <name>Verify app works offline via service worker</name>
  <files>sw.js, index.html</files>
  <action>Verify service worker registers successfully. Confirm cache includes all required assets (HTML, CSS, JS, images, PDF library). Test offline mode - app functions without network.</action>
  <verify>
    <automated>grep -c "fetch\|cache\|install" sw.js</automated>
  </verify>
  <done>Offline mode allows viewing all cached content without network</done>
</task>

<task type="auto">
  <name>Verify app installable on mobile</name>
  <files>index.html, sw.js</files>
  <action>Verify manifest.json link exists in HTML (or inline manifest). Confirm PWA meta tags present (theme-color, viewport). Test install prompt appears on Chrome Android/Safari iOS. Verify app icon displays when installed.</action>
  <verify>
    <automated>grep -c "manifest\|theme-color\|apple-touch-icon" index.html</automated>
  </verify>
  <done>Install prompt appears on compatible browsers, app installs successfully</done>
</task>

</tasks>

<verification>
1. Open app in Chrome Android or Safari iOS
2. Check if install prompt appears
3. Install app - verify icon on home screen
4. Go offline - verify app loads with cached content
</verification>

<success_criteria>
- Service worker caches all required assets
- Offline mode works for all views
- Install prompt appears on compatible browsers
- App installs on mobile device
- All PWA requirements marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-maintenance/01-05-SUMMARY.md`
</output>