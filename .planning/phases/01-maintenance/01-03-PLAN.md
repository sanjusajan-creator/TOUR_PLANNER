---
phase: 01-maintenance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [index.html, js/modules/pdf.js, lib/pdf.js/pdf.min.js]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "PDF tickets viewable with password protection"
    - "Aadhar card documents viewable"
    - "PDF rendering works offline"
  artifacts:
    - path: "index.html"
      provides: "Document viewer view with PDF display area"
      min_lines: 30
    - path: "js/modules/pdf.js"
      provides: "PDF.js wrapper and password logic"
      min_lines: 30
    - path: "lib/pdf.js/pdf.min.js"
      provides: "PDF.js library"
---

<objective>
Maintain and improve document viewing functionality
Purpose: Ensure PDF tickets with passwords and Aadhar viewing work correctly
Output: All document requirements verified functional
</objective>

<execution_context>
@C:/Users/A S U S/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/A S U S/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

From codebase structure:
- js/modules/pdf.js: PDF rendering wrapper
- lib/pdf.js/: PDF.js library files
- index.html: Document viewer section
</context>

<tasks>

<task type="auto">
  <name>Verify PDF tickets viewable with password</name>
  <files>js/modules/pdf.js, index.html</files>
  <action>Verify password prompt displays when viewing tickets. Confirm PDF renders after correct password. Check password validation logic works correctly.</action>
  <verify>
    <automated>grep -c "password\|pdf" js/modules/pdf.js</automated>
  </verify>
  <done>Password prompt displays, renders ticket PDF after correct password</done>
</task>

<task type="auto">
  <name>Verify Aadhar card documents viewable</name>
  <files>index.html, js/modules/pdf.js</files>
  <action>Confirm Aadhar section exists in documents view. Verify Aadhar PDF renders without password (per requirement). Check visibility and clarity.</action>
  <verify>
    <automated>grep -c "aadhar\|document" index.html</automated>
  </verify>
  <done>Aadhar card displays in document viewer without password</done>
</task>

<task type="auto">
  <name>Verify PDF rendering works offline</name>
  <files>lib/pdf.js/pdf.min.js, sw.js</files>
  <action>Verify PDF.js library is cached by service worker. Confirm PDF renders when offline. Check rendering performance doesn't degrade offline.</action>
  <verify>
    <automated>grep -c "pdfjs\|pdf.min" sw.js</automated>
  </verify>
  <done>PDFs render correctly when offline mode is active</done>
</task>

</tasks>

<verification>
1. Open documents view
2. Click ticket - verify password prompt appears
3. Enter password - verify PDF renders
4. Switch to Aadhar - verify no password needed
5. Test offline - verify PDFs still render
</verification>

<success_criteria>
- Tickets require password and render after correct entry
- Aadhar renders without password
- PDFs render offline
- All document requirements marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-maintenance/01-03-SUMMARY.md`
</output>